<div class = "cookbookItem" data-recipid={{_id}}>

	<div class="cookbookRecipe recipe-overlay" data-recipeid={{_id}}>
	  <p>{{name}}</p>
	  <img class="recipe-image" src = {{imageURLs.[1]}}>
	</div>
	<button id="recipe-delete-btn" data-id={{_id}} class="btn btn-lg btn-warning recipe-delete-btn" type="button">Delete</button>
</div>


